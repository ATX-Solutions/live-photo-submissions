import { useState } from 'react';
import { Button, Paper, Typography, Box, Chip } from '@mui/material';

const About = () => {
    return (
        <Box>
            <Typography variant='h5'>About page soon</Typography>
        </Box>
    );
};

export default About;
