import { useState } from 'react';
import { Button, Paper, Typography, Box, Chip } from '@mui/material';

const NotFound = () => {
    return (
        <Box>
            <Typography variant='h5'>Seems like you're lost. </Typography>
        </Box>
    );
};

export default NotFound;
